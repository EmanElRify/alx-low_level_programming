#include "main.h"

/**
 * *_memset - set the memory
 * @s: address of bytes
 * @b: memory area pointer
 * @n: number of bytes
 * Return: a pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
